# 安全说明

## 支持范围

安全修复以当前 `main` 分支和最新正式 Release 为准。旧标签可能不会回溯修复。

## 报告安全问题

请不要在公开 Issue 中披露可利用细节、凭据、个人数据或生产数据库。优先使用 GitHub Security Advisories 的私密报告功能，并提供：

- 受影响版本或提交
- 复现步骤和最小请求
- 影响范围
- 已知缓解措施

不要对非本人控制的生产实例进行扫描、数据访问或破坏性验证。

## 已实现的安全边界

### 认证

- 密码只在服务端处理。
- 使用带随机盐的 `scrypt` 哈希，不保存明文密码。
- 会话 Cookie 为 `HttpOnly`、`SameSite=Lax`。
- 数据库只保存会话令牌的 SHA-256 哈希。
- 修改请求需要独立 CSRF 令牌。
- 登录、注册和改密接口启用速率限制。
- 模拟微信和企业微信认证已删除。

### 授权

- 所有业务服务均由后端执行。
- 服务端校验当前教师、学校范围、资源所有者和管理员角色。
- 学校管理员不能冒充其他教师创建个人资源。
- 文件下载与解析需要登录，并按所有者或学校授权。

### 输入与响应

- API 参数使用 Zod 或显式约束校验。
- 上传限制单文件大小、数量和扩展名。
- 文件使用随机存储名，避免路径穿越和覆盖。
- DOCX 预览 HTML 在服务端经过白名单净化。
- 生产响应启用 CSP、`nosniff`、frame 限制和其他 Helmet 安全头。
- AI API 密钥只从服务端环境变量读取，不返回浏览器。

## 生产部署清单

1. 使用 HTTPS 反向代理。
2. 设置 `INTESCHOOL_COOKIE_SECURE=true`。
3. 保持 `INTESCHOOL_ENABLE_DEMO=false`。
4. 保持 `INTESCHOOL_SEED_DEMO_DATA=false`。
5. 设置至少 12 位的 bootstrap 管理员密码。
6. 首次管理员创建后，从部署配置中删除 bootstrap 密码并重新创建容器。
7. 不将 `.env`、SQLite 文件或上传目录提交到 Git。
8. 限制 `/app/data` 的宿主机访问权限。
9. 定期备份 SQLite 文件和 `uploads/`，并验证恢复。
10. 定期更新镜像并检查 GitHub Security、Dependabot 和 `npm audit`。

## 反向代理

应用只提供 HTTP。TLS 应在可信反向代理终止。代理应：

- 保留 `Host`
- 设置正确的 `X-Forwarded-Proto`
- 传递可信客户端地址
- 限制请求体大小不低于应用配置且不应无限制
- 对登录接口配置额外的边缘速率限制

不要在公网同时暴露反向代理和容器的原始 3000 端口。

## 数据安全

运行数据位于 `/app/data`：

- `inteschool.sqlite`
- `inteschool.sqlite-wal`
- `inteschool.sqlite-shm`
- `uploads/`

文件级备份前应停止应用，或者使用支持 SQLite 在线一致性备份的工具。只复制主数据库而遗漏 WAL 或上传目录可能产生不完整备份。

SQLite 仅支持单实例写入。禁止多个应用容器共享同一数据卷。

## AI 提供商

启用 AI 功能意味着提示文本会发送到配置的第三方提供商。部署者应自行评估：

- 提供商的数据保留政策
- 是否允许发送学生或学校数据
- 所在司法辖区的合规要求
- 模型输出的准确性和版权风险

不要在提示中发送不必要的个人身份信息。AI 生成内容必须由教师审核后使用。

## 已知边界

- 当前为单实例 SQLite 架构，不提供多节点高可用。
- 内建文档识别以规则提取为基础，不能保证所有复杂版式或扫描件准确。
- PDF/DOCX 解析第三方库应随 Release 定期更新。
- 应用不替代学校的正式身份提供商、学生信息系统或合规审计平台。
